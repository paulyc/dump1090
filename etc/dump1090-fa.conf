# dump1090-fa configuration
LATITUDE=51.476853
LONGITUDE=-0.0005002
RECEIVER_OPTIONS=--gain -10
DECODER_OPTIONS=--aggressive --modeac --dcfilter --max-range 400
NET_OPTIONS=--net --forward-mlat
JSON_OPTIONS=--json-location-accuracy 1
OUTPUT_OPTIONS=--quiet --stats --stats-range --stats-every 60
